#include "monty.h"
/**
 * f_stack - Set interpreter mode to stack (LIFO)
 * @head: stack head
 * @counter: line_number
 * Return: no return value
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
